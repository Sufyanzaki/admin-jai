export type PackageDto = {
    id: string;
    name: string;
    price: number;
    image: string;
    validity: number;
    isActive: boolean;
    features: string;
    createdAt: string;
    updatedAt: string;

    soldCount: number;
}
